{
  "name": "flow-finance",
  "version": "1.0.0",
  "description": "Personal expense tracking and financial management app",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && firebase deploy"
  },
  "devDependencies": {
    "terser": "^5.46.0",
    "vite": "^5.0.0"
  }
}
